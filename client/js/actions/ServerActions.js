import * as types from '../constants/ActionTypes';
import ChatWebAPIUtils from '../utils/ChatWebAPIUtils';

export function receiveRawMessage(rawMessages) {

  rawMessages.forEach(function(message) {
  return {
    type: types.ADD_MESSAGE,
    id: id,
    friendID: friendID,
    text: text
  };
}
