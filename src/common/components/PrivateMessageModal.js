import React, { PropTypes } from 'react';
import { Modal } from 'react-bootstrap';

// export default class PrivateMessageModal extends React.Component {

//   render() {

//     return (
//       <div>
//         {PrivateMessageModal}
//       </div>
//     );
//   }
// }

export default const = () => SeparatePrivateMessModal = (
  <div>
    <Modal key={1} show={this.state.privateChannelModal} onHide={::this.closePrivateChannelModal}>
    <Modal.Header>
      {user}
    </Modal.Header>
    <Modal.Body>
      <Button>
        Message
      </Button>
    </Modal.Body>
    <Modal.Footer>
      <Button>
        Close
      </Button>
    </Modal.Footer>
    </Modal>
  </div>
);
